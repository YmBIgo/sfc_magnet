<nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to "SFC Magnet", root_path, :class => "navbar-brand page-scroll" %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a class="page-scroll" href="/about">About</a>
                </li>
                <% if user_signed_in? %>
                    <% if current_user.admin? %>
                        <li>
                            <a href="/admin">ADMIN</a>
                        </li>
                    <% end %>
                    <li>
                        <%= link_to "プロジェクトを探す",ideas_path %>
                    </li>
                    <li>
                        <%= link_to "ユーザーを探す",users_path %>
                    </li>
                    <li>
                        <%= link_to "ユーザーページ", user_path(current_user.id), :class => "page-scroll" %>
                    </li>
                    <li>
                        <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
                    </li>
                <% else %>
                    <li>
                        <%= link_to "ログイン", new_user_session_path, :class => "page-scroll" %>
                    </li>
                    <li>
                        <%= link_to "新規登録", new_user_registration_path, :class => "page-scroll" %>
                    </li>
                <% end %>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>

